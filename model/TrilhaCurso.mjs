export class TrilhaCurso {
    constructor(id_trilha, id_curso, ordem) {
        this.ID_Trilha = Number(id_trilha);
        this.ID_Curso = Number(id_curso);
        this.Ordem = Number(ordem);
    }
}
